<h2>コンテンツ一覧</h2>

<!-- 新規作成リンク(管理者のみ) -->
<% if admin_user? %>
  <%= link_to "新規作成",new_content_path %>
<% end %>

<!-- content_card -->
<div>
  <%= render partial: "layouts/content_card", collection: @contents, as: "content" %>
</div>

<!-- ページネーション  -->
<%= paginate @contents %>